
game:
  log_file: gate.log
  log_level: debug

# 要用k8s，是不是需要global来管理，proxy 作为sidecar呢
# sidecar启动时向global注册服务拉取已有的sidecar列表请求互联
# game之间、game和gate之间的通讯通过sidecar转发
# consul or etcd ？
proxy:
  inst:
    - id: 1
      net: "tcp" # kcp, websocket
      ip: "127.0.0.1"
      port: 49551